package main


func main() {
 // TODOs / Notes:
	// - how-to send mails in go ? receiving might also be necessary (although we can do without, pick whatever is simpler)
	// - Server should be able to: Register, Lookup, Verify a user-key-binding
	//   (either using protocol buffers or some other protocol which we will be using)
	// - should be able recognize that the user clicked on a URL (or the users' client automatically sends an http request which does the same)
	// - (Java's) 'truststore' equivalent in golang ... ?
	//
}
